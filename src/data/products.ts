
// Re-export everything from dataManager for backward compatibility
export * from '@/utils/dataManager';

// Legacy exports for existing code
export const products = [];
export const categories = [];
export const featuredProducts = [];
